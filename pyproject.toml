[project]
name = "christory"
version = "0.1.0"
description = "TUI for searching local Chrome history"
requires-python = ">=3.10"
dependencies = ["textual>=0.86"]

[project.scripts]
christory = "christory:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
